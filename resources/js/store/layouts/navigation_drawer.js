const state = {
    items: [
        { icon: 'mdi-home-outline', text: 'Home', url: '/' },
        { icon: 'mdi-logout', text: 'Log out', url: '/logout' },
    ],
}

const getters = {}
const actions = {}
const mutations = {}


export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations,
}
